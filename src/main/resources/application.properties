spring.application.name=projeto-spring-jwt

# MySQL connection details

# Connection Homologation
spring.datasource.url=jdbc:mysql://square-cloud-db-aaf019ab1983c618ebf4c9.squareweb.app:7201/db_man
spring.datasource.username=squaroud
spring.datasource.password=o3VWpn2u

# Connection Api
#spring.datasource.url=${DB_URL_DEV}
#spring.datasource.username=${DB_USERNAME_DEV}
#spring.datasource.password=${DB_PASSWORD_DEV}

# Driver JDBC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Optional settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Security Properties
security.config.prefix=Bearer
security.config.key=SECRET_KEY
security.config.expiration=3600000

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}

#logs
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

#config port
server.port=80
server.address=0.0.0.0

# Smtp Email
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=matheushard2013@gmail.com
spring.mail.password=f9tKp0kSq6j7FLxP
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


